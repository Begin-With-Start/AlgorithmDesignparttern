<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="designpartern.minifly.com.designparttern">

    <application
        android:allowBackup="true"
        android:icon="@drawable/design"
        android:label="@string/app_name"
        android:roundIcon="@drawable/design"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".designparttern23.FactoryDesignActivity" />
        <activity android:name=".algorithm.AjldActivity" />
        <activity android:name=".algorithm.BinarySearchActivity" />
        <activity android:name=".algorithm.InsertSortingActivity" />
        <activity android:name=".algorithm.BubbleSortingActivity"></activity>
    </application>

</manifest>